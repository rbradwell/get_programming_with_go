package main

// to represent a single unicode codepoint go provides rune which is an alias for the int32 type
/*
func main() {
	var pi rune = 960
	var alpha rune = 940
	var omega rune = 969
	var bang byte = 33
	fmt.Printf("%v %v %v %v\n", pi, alpha, omega, bang) // display numeric values

	fmt.Printf("%c %c %c %c\n", pi, alpha, omega, bang) // display chars represented by numeric values

	// for a char literal also use single quotes
	// the type rune will be infered therefore the following are equivalent

	grade1 := 'A'
	var grade2 = 'A'
	var grade3 rune = 'A'

	fmt.Printf("%c %c %c \n", grade1, grade2, grade3)

	// character literals can also be used with the byte alias

	var star byte = '*'

	fmt.Printf("%c\n", star)
}
*/
