package main

/*
func main() {

	// nil is a zero value
	// a pointer with nowhere to point has value nil
	// the nil identifier is the zero value for slices, maps and interfaces too

	// if a pointer isn't pointing anyhere, attempting to dereference the pointer won't work
	var nowhere *int
	fmt.Println(nowhere)  // <nil>
	fmt.Println(*nowhere) // panic: runtime error: invalid memory address or nil pointer dereference
}
*/
