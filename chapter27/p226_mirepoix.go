package main

/*
func mirepoix(ingredients []string) []string {
	return append(ingredients, "onion", "carrot", "celery")
}

func main() {
	// an empty slice and a nil slice aren't eqivalent, but they can often be use interchangeably
	// the following passed nil to a function that accepts a slice, skipping the step of making a slice

	soup := mirepoix(nil)
	fmt.Println(soup) // [onion carrot celery]
}
*/
