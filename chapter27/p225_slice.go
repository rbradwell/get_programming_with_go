package main

/*
func main() {

	// a slice that's declared without a composite literal or the make built-in
	// will have the value of nil.
	// Fortunately, the range keyword, len built-in, and append built-in all work
	// with nil slices
	var soup []string
	fmt.Println(soup == nil)

	for _, ingredient := range soup {
		fmt.Println(ingredient)
	}

	fmt.Println(len(soup))

	soup = append(soup, "onion", "carrot", "celery")

	fmt.Println(soup)

}
*/
