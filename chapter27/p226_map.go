package main

/*
func main() {
	// as with slices, a map declared without a composite literal or the make built-in has a value of nil.
	var soup map[string]int
	fmt.Println(soup == nil) // prints true
	// maps can evenbe read even whel nil, though writing to a nil map will panic
	measurement, ok := soup["onion"]
	if ok {
		fmt.Println(measurement)
	}
	// if a function only reads from a map it's fine to pass the function nul instead of making an empty map
	for ingredient, measurement := range soup {
		fmt.Println(ingredient, measurement)
	}
}
*/
