package main

/*
func reset(board *[8][8]rune) {
	board[0][0] = 'r'
	/// ...
}

func main() {
	// though slices tend to be preferred over arrays, using arrays can be appropriate when there is no need to change length
	// this shows how pointers allow functions to mutate elements in an array
	var board [8][8]rune
	reset(&board)
	fmt.Printf("%c\n", board[0][0])
}
*/
