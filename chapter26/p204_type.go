package main

/*
func main() {
	answer := 42
	// print the address of the value
	fmt.Println(&answer) // 0xc000016078

	address := &answer
	// dereferencing the pointer
	fmt.Println(*address)

	// in C memory addresses can be manipulated with pointer arithmetic but GO doesn't allow this.

	// the type is a pointer
	fmt.Printf("address is a %T\n", address) // *int
}
*/
