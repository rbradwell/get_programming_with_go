package main

/*
var mu sync.Mutex

func main() {
	// locking and unlocking a mutex
	mu.Lock()         // lock mutex
	defer mu.Unlock() // unlock mutex before returning
	// The lock is held until we return from the function
	// NOTE: without defer we would need to call inlock befire every return statement
}
*/
