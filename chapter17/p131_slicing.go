package main

/*
func main() {
	planets := [...]string{
		"Mercury",
		"Venus",
		"Earth",
		"Mars",
		"Jupiter",
		"Saturn",
		"Uranus",
		"Nepune",
	}
	// slices don't alter the underlying array.  It just creates a view on the array
	terrestrial := planets[0:4]
	gasGiants := planets[4:6]
	iceGiants := planets[6:8]
	fmt.Println(terrestrial, gasGiants, iceGiants)
	// eventhough the above are slices you can still index into slices using an array
	fmt.Println(gasGiants[0])

	iceGiantsMarkII := iceGiants // NOTE: this just creates another slice, not a new copy of the array
	iceGiants[1] = "Poseidon"    // this alters the underlying array
	fmt.Println(planets)
	fmt.Println(iceGiants, iceGiantsMarkII)
}
*/
