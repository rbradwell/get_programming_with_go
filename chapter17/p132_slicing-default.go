package main

/*
func main() {
	planets := [...]string{
		"Mercury",
		"Venus",
		"Earth",
		"Mars",
		"Jupiter",
		"Saturn",
		"Uranus",
		"Nepune",
	}
	// ommiting the first index in a slice defaults to the first item in the array
	terrestrial := planets[:4]
	gasGiants := planets[4:6]
	// ommiting the last index in a slice default to the last item in the array
	iceGiants := planets[6:]
	allPlanets := planets[:]
	fmt.Println(terrestrial, gasGiants, iceGiants)
	fmt.Println(allPlanets)
	// slicing syntax also works on strings
	neptune := "Neptune"
	tune := neptune[3:]
	fmt.Println(tune) // tune
}
*/
