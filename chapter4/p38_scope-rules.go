package main

/*
var era = "AD" // available throughout package
// NOTE: Short declarations are not available for variables declare in the package scope
func main() {
	year := 2018
	// era and year are in scope
	switch month := rand.Intn(12) + 1; month {
	// era, year and month are in scope
	case 2:
		// era, year, month and day are in scope
		day := rand.Intn(28) + 1
		fmt.Println(era, year, month, day)
	case 4, 6, 9, 11:
		day := rand.Intn(30) + 1 // its a new day
		fmt.Println(era, year, month, day)
	default:
		day := rand.Intn(31) // its a new day
		fmt.Println(era, year, month, day)
	}
	// month and year are out of scope
}

// year is no longer in scope
*/
