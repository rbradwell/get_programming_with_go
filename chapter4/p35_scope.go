package main

/*
func main() {
	//	var count = 0
	count := 0 // this is an alternative way to declare the above line.
	for count < 10 {
		// a new scope begins
		var num = rand.Intn(10) + 1
		fmt.Println(num)
		count++
		// this scope ends
	}

	//	fmt.Print(num) // undefined
}
*/
