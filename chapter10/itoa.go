package main

/*
func main() {
	// integer to ASCII
	countdown := 10
	// itoa stands for integer to ascii.  Unicode is a superset of the old ASCII standard

	// method 1
	str := "Launch in T minus " + strconv.Itoa(countdown) + " seconds."
	fmt.Println(str)

	// method 2
	str2 := fmt.Sprintf("Launch in T minus %v seconds. ", countdown)
	fmt.Println(str2)

	// from string to integer

	countdown2, err := strconv.Atoi("10")
	if err != nil {
		// oh no something went wrong
	}
	fmt.Println(countdown2)

	// in GO, once a variable is declared it has a type and the type cannot be changed.  This is static typing.
}
*/
