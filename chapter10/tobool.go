package main

/*
func main() {
	// convert a string to a boolean
	yesNo := "no"
	launch := (yesNo == "yes")
	fmt.Println("Ready for launch:", launch) // ready for launch is false

	// go will report an error if you attempt to convert a Boolean with string(false), int(false),
	// bool(1) or bool(yes)
	// go does not have a numerical equivalent for true and false as in some other languages
}
*/
