package main

/*
// dump slice length, capacity, and contents
// cap is a built in function that will give the capacity of the slice
func dump(label string, slice []string) {
	fmt.Printf("%v: length %v, capacity %v %v\n", label, len(slice), cap(slice), slice)
}

func main() {
	// slice on an array
	dwarfs := []string{
		"Ceres",
		"Pluto",
		"Haumea",
		"Makemake",
		"Eris",
	}

	dump("dwarfs", dwarfs)          // dwarfs: length 5, capacity 5 [Ceres Pluto Haumea Makemake Eris]
	dump("dwarfs[1:2", dwarfs[1:2]) // dwarfs[1:2]: length 1, capacity 4 [Pluto]
}
*/
