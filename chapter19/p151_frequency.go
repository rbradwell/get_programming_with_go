package main

/*
func main() {

	temperatures := []float64{
		-28.0, 32.0, -31.0, -29.0, -23.0, -29.0, -28.0, -33.0}

	// unless maps are initialised with a composite literal then allocate with make
	frequency := make(map[float64]int)

	// also could preallocate space for the number of keys like this .e.g. for 8 keys
	frequency = make(map[float64]int, 8)

	for _, t := range temperatures {
		frequency[t]++
	}

	for t, num := range frequency {
		fmt.Printf("%+.2f occurs %d times\n", t, num)
	}
}
*/
