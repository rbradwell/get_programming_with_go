package main

/*
func main() {
	// declaring a map
	temperature := map[string]int{
		"Earth": 15,
		"Mars":  -65,
	}
	temp := temperature["Earth"]
	fmt.Printf("On average the Earth is  %v degrees C.\n", temp)
	temperature["Earth"] = 16
	temperature["Venus"] = 464
	fmt.Println(temperature) // map[Earth:16 Mars:-65 Venus:464]

	// if you access a key that doesn't exist in the map the result is zero value for that type .i.e. 0 for int
	moon := temperature["Moon"]
	fmt.Println(moon) // 0

	// the following shows how to destinguish between Moon not existing in the map versus being in the map but with a value zero

	if moon, ok := temperature["Moon"]; ok { // the comma ok syntax
		fmt.Printf("On average the moon is %v degrees C.\n", moon)
	} else {
		fmt.Println("Where is the moon?")
	}

}
*/
