package main

/*
func main() {
	// declare array of length 8
	var planets [8]string

	planets[0] = "Mercury"
	planets[1] = "Venus"
	planets[2] = "Earth"
	earth := planets[2]
	fmt.Println(earth)

	fmt.Println(len(planets))     // 8 - NOTE: len is a built in string that doesn't require import
	fmt.Println(planets[3] == "") // true i.e. array elements initialised to empty string

	//	planets[8] = "Pluto" // out of bounds panic runtime error
}
*/
