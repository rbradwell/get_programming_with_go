package main

/*
func main() {
	// using a composite literal to initialise an array (or any composite type)
	dwarfs := [5]string{"Ceres", "Pluto", "Haumea", "Makemake", "Eris"}
	// rather than declaring an array of 5 you can do this so that the length of the array matches the number of elements
	dwarfs = [...]string{"Ceres", "Pluto", "Haumea", "Makemake", "Eris"}

	for i := 0; i < len(dwarfs); i++ {
		dwarf := dwarfs[i]
		fmt.Println(i, dwarf)
	}
}
*/
